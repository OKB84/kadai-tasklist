<% if logged_in? %>
  <p>ログイン完了ユーザー：<%= current_user.name %></p>  
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Welcome to the TaskList!</h1>
    </div>
  </div>
  
  <div class="text-center">
    <div>
      <%= link_to 'ログイン', login_path, class:'btn btn-primary btn-lg' %>
    </div>
    <%= link_to 'はじめての方はこちら', signup_path %>
  </div>
<% end %>

